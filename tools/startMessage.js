//import colors from 'colors';

/* eslint-disable no-console */

if(process.env.NODE_ENV == 'prod'){
    console.log('Starting app in PROD mode...');//.green);
} else {
    console.log('Starting app in dev mode...');//.green);
}

